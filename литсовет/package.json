{
  "name": "litsovet",
  "version": "1.56.0",
  "description": "Литсовет — ИИ-инструмент для написания прозы",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --no-warnings test/parsers.test.mjs test/invariants.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
